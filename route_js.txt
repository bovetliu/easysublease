JSON from server

var newFeatureOptions = C_travel_control_pointer.createFeatureOptions(C_travel_control_pointer._array1[i])

in function of createFeatureOptions {
....

newLineString = new google.maps.Data.LineString( google.maps.geometry.encoding.decodePath( prop_array['encoded_polyline'] )  );
//decodePath will return ary:latlngs

tbr = {properties:properties_1, id:prop_array['id'], geometry:newLineString};
//一个 FeatureOption包含 properties, id, and geometry
....
}

谈后 map_cs.data.add(newFeatureOptions); //

修改前存了 05060517pm